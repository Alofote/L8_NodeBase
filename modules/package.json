{
  "name": "modules",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "MODE=production node -r dotenv/config index.js dotenv_config_path=.env.production",
    "start": "MODE=development node -r dotenv/config index.js dotenv_config_path=.env.development",
    "deploy": "MODE=domain node -r dotenv/config index.js dotenv_config_path=.env.domain",
    "dev": "nodemon index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^17.2.3",
    "nodemon": "^3.1.11"
  }
}
